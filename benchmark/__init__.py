"""GPU LLM Systems Lab benchmark package.

Bootstrap stage: this package currently exposes metadata only. Configuration
models, the result schema, inventory collection, and the runner are added by
later tasks (see tasks/).
"""

__version__ = "0.0.0"

__all__ = ["__version__"]
